version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema.sql"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "dal"
        out: "sql/dal"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true